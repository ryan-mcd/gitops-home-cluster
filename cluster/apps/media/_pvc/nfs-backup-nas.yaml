---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: nfs-backup-nas-pv
spec:
  storageClassName: backup-nas
  capacity:
    storage: 1Mi
  accessModes:
  - ReadWriteMany
  persistentVolumeReclaimPolicy: Retain
  nfs:
    server: ${NAS_ADDRESS}
    path: /mnt/user/k8s-bak
  mountOptions:
  - tcp
  - intr
  - hard
  - noatime
  - nodiratime
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-backup-nas-pvc
  namespace: media
spec:
  accessModes:
  - ReadWriteMany
  storageClassName: backup-nas
  resources:
    requests:
      storage: 1Mi
